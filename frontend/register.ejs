<!doctype html>
<html lang="es" data-theme="default">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Registrarse — Klay-Up</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/auth/register.css">
</head>
<body>

<!-- Video de fondo -->
  <video class="video-background" autoplay muted loop playsinline>
    <source src="/imagen/video.mp4" type="video/mp4">
    Tu navegador no soporta videos HTML5.
  </video>

  <main class="auth-page" id="authPage">
    <div class="auth-wrapper">
      <img src="/imagen/klay.png" alt="Klay logo" class="auth-logo" />
      <h1>Crear cuenta</h1>
      <p>Regístrate para empezar a subir fotos y videos</p>
      <form id="registerForm" method="post" action="/auth/register" enctype="multipart/form-data" accept-charset="utf-8">
        <input type="text" name="full_name" placeholder="Nombre completo" required>
        <input type="email" name="email" placeholder="Correo" required>
        <input type="password" name="password" placeholder="Contraseña" required>
        <div id="registerError" class="form-error" aria-live="polite" style="display:none;"></div>
        <label>Avatar (elige uno)</label>
        <div class="avatar-picker-wrap" style="position:relative;display:flex;gap:8px;align-items:center;margin-bottom:8px;">
          <div style="position:relative;">
            <button type="button" id="avatarPickerBtn" class="avatar-picker-toggle" aria-haspopup="dialog" aria-expanded="false">
              <span class="avatar-btn-inner">
                <img id="avatarPreview" src="<%= (localAvatars && localAvatars.length) ? ('/imagen/avatares/' + encodeURIComponent(localAvatars[0])) : '/imagen/klay.png' %>" alt="Avatar seleccionado" />
                <svg class="person-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                  <path d="M12 12c2.761 0 5-2.239 5-5s-2.239-5-5-5-5 2.239-5 5 2.239 5 5 5z" fill="#fff" opacity="0.9"/>
                  <path d="M4 20c0-4 4-6 8-6s8 2 8 6v1H4v-1z" fill="#fff" opacity="0.9"/>
                </svg>
              </span>
            </button>
            <div id="avatarPicker" class="avatar-picker" aria-hidden="true" style="display:none;">
              <div class="picker-grid">
            <% if (localAvatars && localAvatars.length) { %>
                <% localAvatars.forEach(function(file, idx) { %>
                  <button type="button" class="avatar-thumb <%= idx===0 ? 'selected' : '' %>" data-src="/imagen/avatares/<%= encodeURIComponent(file) %>">
                    <img src="/imagen/avatares/<%= encodeURIComponent(file) %>" alt="avatar" />
                  </button>
                <% }); %>
            <% } else { %>
              <p style="margin:8px 0;color:#6b7280">No hay avatares locales disponibles.</p>
            <% } %>
              </div>
            </div>
          </div>
          <input type="hidden" name="default_avatar" id="defaultAvatar" value="<%= (localAvatars && localAvatars.length) ? ('/imagen/avatares/' + encodeURIComponent(localAvatars[0])) : '' %>">
        </div>
        <div style="display:flex;gap:8px;margin-top:8px;">
          <button type="submit">Crear cuenta</button>
        </div>
      </form>
      <p style="margin-top:12px;">¿Ya tienes cuenta? <a href="/login">Ingresar</a></p>
    </div>
  </main>

  <script>
    window.SUPABASE_URL = "<%= SUPABASE_URL %>" || '';
    window.SUPABASE_ANON_KEY = "<%= SUPABASE_ANON_KEY %>" || '';
    window.API_BASE = window.location.origin;
  </script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js/dist/umd/supabase.min.js"></script>
  <script src="/auth/auth.js" defer></script>
</body>
</html>